{% extends 'base.html.twig' %}

{% block title %}ReferenceEntre{% endblock %}

{% block body %}
    <h1>ReferenceEntre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reference_entre.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ reference_entre.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reference_entre_index') }}">back to list</a>

    <a href="{{ path('reference_entre_edit', {'id': reference_entre.id}) }}">edit</a>

    {{ include('reference_entre/_delete_form.html.twig') }}
{% endblock %}
