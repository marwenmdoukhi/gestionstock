{% extends 'base.html.twig' %}

{% block title %}Vente{% endblock %}

{% block body %}
    <h1>Vente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vente.id }}</td>
            </tr>
            <tr>
                <th>DateFacture</th>
                <td>{{ vente.dateFacture ? vente.dateFacture|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>MontantFact</th>
                <td>{{ vente.montantFact }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ vente.qte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vente_index') }}">back to list</a>

    <a href="{{ path('vente_edit', {'id': vente.id}) }}">edit</a>

    {{ include('vente/_delete_form.html.twig') }}
{% endblock %}
