<form method="post" action="{{ path('reference_achat_delete', {'id': reference_achat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reference_achat.id) }}">
    <button class="btn">Delete</button>
</form>
