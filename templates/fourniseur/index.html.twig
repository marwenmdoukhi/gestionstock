{% extends 'base.html.twig' %}

{% block title %}Fourniseur index{% endblock %}

{% block body %}
    <h1>Fourniseur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>RaisionSocial</th>
                <th>Datecreation</th>
                <th>Matruclefiscal</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fourniseur in fourniseurs %}
            <tr>
                <td>{{ fourniseur.id }}</td>
                <td>{{ fourniseur.raisionSocial }}</td>
                <td>{{ fourniseur.datecreation ? fourniseur.datecreation|date('Y-m-d') : '' }}</td>
                <td>{{ fourniseur.matruclefiscal }}</td>
                <td>
                    <a href="{{ path('fourniseur_show', {'id': fourniseur.id}) }}">show</a>
                    <a href="{{ path('fourniseur_edit', {'id': fourniseur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('fourniseur_new') }}">Create new</a>
{% endblock %}
